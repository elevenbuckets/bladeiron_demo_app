module.exports =
{
        Greeter_newGreet_sanity(addr, jobObj)
        {
                return true;
        },

        Greeter_kill_sanity(addr, jobObj)
        {
                return false;
        }
}
